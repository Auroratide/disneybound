# Next.js environment variables.
# Copy this file to .env.local and fill in your values.
#
# For Pocketbase/Docker secrets, see pocketbase/.env.example instead.

NEXT_PUBLIC_POCKETBASE_URL=http://127.0.0.1:8090

# PocketBase superuser credentials — server-side only (no NEXT_PUBLIC_ prefix).
# Used by API routes that need admin access (e.g. creating users on first OTP login).
# Must match pocketbase/.env.local.
PB_SUPERUSER_EMAIL=admin@disneybounding.com
PB_SUPERUSER_PASSWORD=password
