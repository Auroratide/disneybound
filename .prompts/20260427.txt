user: On my server I see in the logs that it is failing with a 400 on "http://127.0.0.1:8090/api/collections/_superusers/auth-with-password". The username and password look correct (they're the same as what I use to log into the admin portal). What might be wrong?

user: You're right that pm2 env does not show my environment variables. However, I also have .env.local defined with the values in them. Why isn't it picking it up?

user: Ok I now see the environment variables in pm2 env, but it still returns the same 400 error. Is http://127.0.0.1 correct or am I maybe missing something else?

user: When I first created Pocketbase I had accidentally used the wrong password in the env file. I've since changed it in the .env file and also changed it in the admin dashboard for good measure, and I've restarted the container (but did not recreate it from scratch). Is that a problem? Do I need to recreate it from scrtach?

user: If I change the env in the ecosystem file what do I need to do for that to be reflected in pm2?

user: That didnt work.

user: Ok I got that all working, finally. Now I have a new error. I think I've uploaded my avatar correctly, but it doesn't show up on the site and the server has this error: upstream image http://127.0.0.1:8090/api/files/_pb_users_auth_/06c1vdov52p2btz/auroratide_avatar_bg_512_xqe3a2nojq.png resolved to private ip ["127.0.0.1"]

user: I am getting this error on CI, and when I try to install playwright it fails because playwright isnt a dependency. Do I need to npm install playwright now? Looks like Playwright Test or Playwright was just installed or updated. ║
║ Please run the following command to download new browsers:              ║
║                                                                         ║
║     pnpm exec playwright install                                        ║
║                                                                         ║
║ <3 Playwright Team

user: The confirm-otp returns a token and record on success, and flips verified to true for a new user test seems to be flakey. Why?

user: /commit

user: Ah ok, I think the diagnosis might be right but not the solution? My local pocketbase is configured with a .env.local file which defines PB_SMTP_HOST and PB_SMTP_PORT. That file is gitignored so it isn't on CI. Rather than making the test responsible for this, I think it should be done on the startup of the container on CI.

user: I noticed that when I build the app, it generates the home page as prerendered static content. However, that page is supposed to dynamically get characters. Why is next doing this? How do I tell it that the page needs to get new content? Or is the problem that the fetch shouldn't be happening server side?

user: Ok that makes sense. Two questions: 1) Can I force a revalidation? 2) Does this happen on the character pages for which users can upload outfits? Their outfits should appear immediately, not after an hour.

user: /commit

user: So while playing with the color picker against the image, I noticed that it seems to be off by quite a lot, meaning it is not choosing the color where I click but is choosing the color from somewhere else in the image.

user: Ok that's better, but I did like how it use to fit without scrolling. Is it possible to accomplish that?

user: /commit

user: When the user is choosing a color off the image, can you add like a little circle bottom-right of the cursor indicating what color is currently being hovered? On mobile there is no hover, so this shouldn't exist.

user: looks great, make it a little bigger please!

user: /commit

user: The revalidate path fix is breaking the tests. Why?

user: /commit

user: Btw on CI I am seeing that when deploying via ssh, I get pnpm command not found. Is that because pnpm has to be on the CI machine? pnpm is definitely on the VPS.

user: When someone uploads their outfit and it shows them a preview of it before confirmation, the preview should be cover and with a white background, same as the photo component.

user: /commit against the image, I noticed that it seems to be off by quite a lot, meaning it is not choosing the color where I click but is choosing the color from somewhere else in the image.

user: Ok that's better, but I did like how it use to fit without scrolling. Is it possible to accomplish that?

user: /commit My local pocketbase is configured with a .env.local file which defines PB_SMTP_HOST and PB_SMTP_PORT. That file is gitignored so it isn't on CI. Rather than making the test responsible for this, I think it should be done on the startup of the container on CI.

user: I noticed that when I build the app, it generates the home page as prerendered static content. However, that page is supposed to dynamically get characters. Why is next doing this? How do I tell it that the page needs to get new content? Or is the problem that the fetch shouldn't be happening server side?

user: Ok that makes sense. Two questions: 1) Can I force a revalidation? 2) Does this happen on the character pages for which users can upload outfits? Their outfits should appear immediately, not after an hour.

user: /commit
