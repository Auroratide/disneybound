that makes sense. Please use these values in the test

commit

Looking at the community outfits, the usernames and avatars are still saying anonymous. Why is that, can you fix it, and can you write a test somewhere that proves you fixed it? (start with the test, aka test driven development)

Are there any security considerations I need to make with regard to these rules? For example, we must avoid at all costs people arbitrarily querying pocketbase for a list of users and getting their email addresses.

Great this is working! I want to be double sure that we are not leaking anything. In data, I want you to create a test file called "security.test.ts" which we are going to use to do some basic penetration testing, pretending to be a (non-admin) user using the pocketbase sdk to probe our system for weakness. Start with a test that ensures we cannot list users, and a test that ensures we cannot get user's emails (unless we _are_ that user).

I think the tests are failing. Can you run them and diagnose?

I deleted the user myself.

Ran the migration for you

commit

Next, let's make it so when the user clicks on a community outfit, it full screens it. I want this done a certain way, using the img-zoom web component described here: https://components.auroratide.com/img-zoom

Looks like you added the img-zoom tag right, but there's a bit of a strange behaviour happening during the transition. I think it's because the img tag has this style on it: "position: absolute; height: 100%; width: 100%; inset: 0px; color: transparent;". But where is this style coming from and how can I modify it?

commit

Taking a look at the account page, there are some adjustments I want to make. Make the sidebar white background. Align the email to the left. Make the sidebar a bit wider.

I want to pivot on the design of the account page. I've updated the design in .plans/accounts-page.png, so that the profile info is now a banner above the outfits. The design is pretty crude so don't follow it EXACTLY, just use it as a guide for layout.

commit

Rename the /account page to /profile

commit

I notice the profile page and the character pages have a polaroid-like photo display for outfits. I want to make that a common component between them, for consistency. Please make this component and use it in both places.

Good so far. I want a slightly different abstraction for the showUser boolean. For that caption area, on the character pages I want it to be the user and avatar, but on the profile page I want it to show the name of the character being disney bounded. Change the component to allow for that abstraction and then apply it.

Great. For accessibility, let's also change the outfit card to use figure and figcaption.

Two more things. 1) Make the caption a little larger (for accessibility font size must be a minimum of 16px, and right now it's at 14.4px). 2) On the profile page, make the caption link to the character's page.

commit
