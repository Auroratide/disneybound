I want to tweak the suggest-a-character flow. Flip steps 2 and 3: Uploading the image should be the second step, and choosing colors should be the third step.

---

/commit

---

For the colors, I actually don't care about the "how to wear it" part of the form. Remove it from the form, and from the data model. Instead, I want to name the colors "Primary", "Secondary", and "Accessory", so it's more clear what colors users should choose.

---

/commit

---

Ok, for choosing colors, I want to help users a little bit. Instead of letting choose colors completely from scratch, I want to give them access to the image they just uploaded, and let them use a color picker to get at least the initial color they want. This is why I moved the image bit to Step 2. Before implementing, give me suggestions of how this could look from a user experience perspective.

---

How would Option A look on mobile?

---

Ok, let's go with Option A2.

---

Keep the type="color" input, it should be there for fine-tuning or as a fallback.

---

Very nice! Some tweaks: 1) I don't need a swatch div next to the color input box. Just make the color image box bigger and rectangular. 2) The "Pick" Option needs to be more specific; make it say "Pick from Image", even on mobile. 3) Dialogs like the one you made should use the `dialog` HTML element for accessibility reasons; this is what the LoginModal does.

---

A test is failing, can you see why and fix it or tell me if I need to do something?

---

/commit

---

More tweaks to the suggest a character page: 1) Center the title and subtitle. 2) The step numbers should be spaced so they take up the whole width of the container. 3) The step titles should also be visible on mobile, but to make it fit, maybe place them under the number rather than beside. 4) The inputs should have a white background. 5) On the Review Submission page, use the character card like on the character pages so that users get an accurate preview of what it will look like. 6) The "Your submission will appear after review. You won't be able to edit it after submitting." text needs to be bigger.

---

Close but you missinterpretted tweak 5. Use the character banner as seen on the individual character page, not the front page.

---

Great, that's the right component now. However, it looks squished because the suggestion form is thinner than the actual character pages. Can you fix that? I do like how you're using a thinner container for most of the form, as it fits nicely, but for the character preview we need more space. It looks fine on mobile since the component is responsive.

---

/commit
