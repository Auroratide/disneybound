# Copy this file to pocketbase/.env.local and fill in your values.
# This file is only used by Docker Compose — Next.js never sees it.

PB_SUPERUSER_EMAIL=admin@disneybounding.com
PB_SUPERUSER_PASSWORD=password

# SMTP — used by migration 005 to configure PocketBase email sending.
# In dev, point at the local Mailpit container (browse captured emails at localhost:8025).
# In production, set to your real SMTP provider; omit to configure via the admin UI instead.
PB_SMTP_HOST=mailpit
PB_SMTP_PORT=1025
# PB_SMTP_USERNAME=
# PB_SMTP_PASSWORD=
# PB_SMTP_AUTH_METHOD=PLAIN
# PB_SMTP_TLS=false
