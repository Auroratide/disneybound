"use client";
// Registers the <img-zoom> custom element on the client.
// Render this once anywhere in the tree before <img-zoom> is used.
import "@auroratide/img-zoom/lib/define.js";

export function ImgZoomRegistrar() {
  return null;
}
